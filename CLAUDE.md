# 개인 프로젝트 작업 지침

작업 시작 시 루트 `AGENTS.md`를 읽고 공통 지침을 따른다.
worklog는 `docs/WORKLOG_POLICY.md`에 따라 월별 폴더에서 작업 단위로 관리한다. 매 대화마다 새 항목을 만들지 않는다.

- `ai-input/` 전체는 항상 Git에서 제외한다. `git add -f`로 추가하거나 원격에 업로드하지 않는다.
