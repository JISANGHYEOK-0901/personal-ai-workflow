# 개인 프로젝트 작업 지침

## 작업 기록

- 작업 시작 시 `docs/WORKLOG_POLICY.md`와 관련 작업의 기존 기록을 확인한다.
- worklog는 대화 턴이 아닌 **독립된 목표·산출물을 가진 작업 단위**로 관리한다. 같은 작업의 질문·답변·결정·실행은 하나의 작업 ID에 모은다.
- 월별 폴더와 날짜별 파일(`ai-input/worklog/YYYY-MM/YYYY-MM-DD.md`)을 유지한다. 기존 작업은 날짜·월·세션이 바뀌어도 원래 항목을 갱신한다.
- 완료·보류·중단·실패, 중요한 범위·결정·상태 변화, 사용자 명시 기록 요청 때 관련 항목을 생성·갱신한다. 매 답변마다 기록하지 않는다. 같은 상태·내용이면 파일과 시각을 바꾸지 않는다.
- 코드 변경 없는 조사·기획도 독립된 목표가 있으면 작업이다. 단순 확인·목표 재진술·진행 질문은 새 작업으로 만들지 않는다.
- 사용자 확정, AI 제안, 실제 실행 결과를 구분하고 증거·잔여 사항을 남긴다. 답변을 마쳤다는 이유로 전체 작업을 완료 처리하지 않는다.
- 비밀값은 기록하지 않고 과거 시각·비용·토큰은 추정하지 않는다. 기록 실패 시 알린다.
- 적용 범위는 현재 프로젝트다. 다른 프로젝트의 쓰기 권한을 부여하지 않는다.

상세 형식의 정본: `docs/WORKLOG_POLICY.md`.

- `ai-input/` 전체는 항상 Git에서 제외한다. `git add -f`로 추가하거나 원격에 업로드하지 않는다.
